version: "3.9"
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: st-k8s:latest
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    env_file:
      - .env.local
